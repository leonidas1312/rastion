[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rastion-sa"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24", "rastion"]

[project.entry-points."rastion.solvers"]
sa = "rastion_sa.solver:get_solver"
